# Zone de limitation de débit, déclarée au niveau http (conf.d).
# 10 requêtes par minute par adresse IP sur /api/v1/auth/*, rafale de 10 tolérée.
limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;
limit_req_status 429;
